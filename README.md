# training

## frontEnd（ReactJS）

``` bash
# install dependencies
npm install

# serve with hot reload at localhost:3000
npm run start
```

## backEnd（NodeJS）

``` bash
# install dependencies
npm install

# serve with start at localhost:4000
node app.js
```
